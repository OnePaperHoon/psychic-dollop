import React from "react";
import { createRoot } from "react-dom/client";
import PsychicDollop from "./PsychicDollop.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PsychicDollop />
  </React.StrictMode>
);
